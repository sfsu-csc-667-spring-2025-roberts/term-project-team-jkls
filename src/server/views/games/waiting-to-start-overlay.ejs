<div id="game-table-waiting-overlay">
  <% if(isHost) { %>
  <button id="start-game-button" class="shown">Start Game</button>
  <% } else { %>
  <strong class="waiting-for-host">Waiting for host...</strong>
  <% } %>
</div>